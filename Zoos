#include<string.h>
#include<stdio.h>
main()
{
	char str[20],z[]="z";
	int i,count1 = 0,count2 = 0	;
	gets(str);
	for(i=0;i<strlen(str);i++)
	{
		if(str[i] == z[0])
		 count1++;
		else
		 count2++;
	}
	count1=count1*2;
	if(count1 == count2)
		printf("Yes");
	else
		printf("No");
}
